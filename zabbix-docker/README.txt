Clone zabbix-docker project
git clone https://github.com/zabbix/zabbix-docker.git

User following command to setup zabbix server containers:
docker-compose -f docker-compose_v3_alpine_pgsql_latest.yaml up -d
